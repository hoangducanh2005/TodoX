server.js đang dùng cú pháp ES Module:

import express from 'express';

-------
package.json là file khai báo cấu hình của project Node.js 📦
Nó giống như CMND của project backend/frontend: nói cho Node biết project này là gì, dùng thư viện nào, chạy bằng lệnh nào.
------
mongodb+srv://... của MongoDB Atlas cần giải tên host qua DNS SRV record.

Khi bạn thêm (và không comment) hai dòng:

thì Node sẽ dùng Google DNS để giải tên, và cluster0.7mdbnbk.mongodb.net được tìm thấy, nên kết nối thành công.

Nếu không có dòng đó, Node sẽ dùng DNS mặc định của hệ thống bạn. Nếu DNS mặc định không trả về được record SRV cho Atlas, thì sẽ bị:
Error connecting to MongoDB

Tóm lại
Không phải vì hai dòng đó “kết nối được database”, mà vì chúng sửa lại DNS resolver trước khi Mongoose kết nối.

-----
Frontend dependencies
 npm install react-router sonner axios lucide-react
 4 package ta se can sonner de hien noti dang pop up
 react router: quản lý định tuyến (routing) trong các ứng dụng React.
Nói một cách đơn giản, nếu một website thông thường chuyển trang bằng cách tải lại toàn bộ trình duyệt (Reload), 
thì React Router giúp ứng dụng của bạn chuyển đổi giữa các "trang" (Component) một cách mượt mà mà không cần tải lại trang.
 axious : gui va nhan data qua api
 lucide : icon 

 Toast
 Trong thư viện Sonner, Toast chính là cái "thông báo nhỏ" (pop-up) nhảy lên ở góc màn hình để phản hồi cho người dùng biết 
 một hành động nào đó đã thành công, thất bại hoặc đang xử lý.
 2. Các thành phần chính khi dùng Sonner
Để dùng được Toast, Sonner chia làm 2 phần:

<Toaster />: Là "cái khay" chứa các thông báo. Bạn chỉ cần đặt nó một lần duy nhất ở file cha (như App.jsx hoặc Layout.jsx).

toast(): Là hàm để bạn "bắn" ra thông báo bất cứ khi nào cần.

import { Toaster, toast } from 'sonner';

function App() {
  return (
    <div>
      <Toaster position="top-right" richColors /> {/* Đặt cái khay ở đây */}
      
      <button onClick={() => toast.success('Đã gửi tin nhắn!')}>
        Gửi ngay
      </button>
    </div>
  );
}

-----
shadcn/ui là một bộ component UI cho React.
Nó cung cấp sẵn các thành phần giao diện đẹp như Button, Card, Dialog, Form, Table, Dropdown...
Điểm đặc biệt

shadcn/ui không phải thư viện component kiểu cài xong import từ package như MUI.

Nó sẽ copy code component vào project của bạn, nên bạn có thể sửa thoải mái.

Dùng với gì?

Thường dùng với:

React
Next.js
Vite
Tailwind CSS
Ví dụ component
npx shadcn@latest add button

Sau đó dùng:

import { Button } from "@/components/ui/button";

<Button>Add Task</Button>